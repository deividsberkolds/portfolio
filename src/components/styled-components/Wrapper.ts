import styled from 'styled-components';

export const Wrapper = styled.section`
  padding: 1em;
  text-align: center;
  max-width: 1200px;
  margin: 0 auto;
  display: flex;
  flex-direction: column;
`;
